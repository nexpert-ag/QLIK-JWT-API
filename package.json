{
  "name": "qlik-jwt-vercel-api",
  "version": "1.0.0",
  "description": "Vercel serverless function to generate JWT tokens for Qlik Cloud",
  "main": "api/generate-token.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
